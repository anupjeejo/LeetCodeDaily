class Solution {
public:
    int maxDepth(string s) {
        int n = s.size();
        stack<char> st;
        int result = 0;

        for(int i=0; i<n; i++)
        {
            if(s[i] == '(')
…            }

        }
        return result;
    }
};